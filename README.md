# LeafGuard-DeepLearningProject
LeafGuard is an AI-powered plant disease detection system that uses a custom CNN model to classify leaf images into 38+ disease and healthy categories. The Streamlit app provides real-time predictions, confidence scores, and AI-generated cure suggestions, while also tracking prediction patterns through a built-in drift monitoring dashboard.
